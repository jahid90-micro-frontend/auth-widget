{
    "version": "4",
    "commands": {
        "build": "CI=true pnpm build",
        "lint": "pnpm lint",
        "test": "pnpm test",
        "start": "pnpm start",
        "docker:build": "docker build -t auth:dev -f Dockerfile.dev .",
        "docker:start": "docker-compose up -d",
        "docker:stop": "docker-compose down",
        "prod:build": "PUBLIC_URL=/minance docker build -t registry.jahiduls.mint/auth-widget:local -f Dockerfile ."
    }
}
